import { Text, ScrollView } from "react-native";
import { Screen } from "../components/Screen";

export default function About() {
  return (
    <Screen>
      <ScrollView className="h-full bg-black"></ScrollView>
    </Screen>
  );
}
